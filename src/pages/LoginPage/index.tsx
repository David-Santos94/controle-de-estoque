import FormLogin from '../../containers/FormLogin'
import { FlexContainer } from '../../styles'

const LoginPage = () => {
  return (
    <FlexContainer>
      <FormLogin />
    </FlexContainer>
  )
}

export default LoginPage
