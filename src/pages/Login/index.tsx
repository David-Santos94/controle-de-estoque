import FormLogin from '../../containers/FormLogin'
import { FlexContainer } from '../../styles'

const Login = () => {
  return (
    <FlexContainer>
      <FormLogin />
    </FlexContainer>
  )
}

export default Login
